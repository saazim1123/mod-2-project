<h1> New Review </h1>

<%= form_for([@game, @game.reviews.build]) do |f| %>
  
  <%= f.label :rating%>
  <%= f.file_field :rating%><br><br>

   <%= f.label :comment%>
  <%= f.file_field :comment%>

  <%= f.button :submit %>



<% end %>
