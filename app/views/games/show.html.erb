<h1><%= @game.title%></h1>

<div class ="row">

    <div class = "col-md-4">
    <%= render @game.reviews %>

    <%if user_signed_in? %>
        <h3><%= link_to "Write a review for #{@game.title}", new_game_review_path(@game) %></h3>
        <%if Review.find_by(user_id: current_user.id) %>    
        <%end%>
    <%end%>

    </div>

</div>


